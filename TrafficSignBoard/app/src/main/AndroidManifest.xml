<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.montclair.mhaskep1.registerandlogin">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/quiz_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/quiz_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".rent"></activity>
        <activity android:name=".sell" />
        <activity android:name=".buy" />
        <activity android:name=".ResultAnalysis" />
        <activity android:name=".Questions" />
        <activity android:name=".LandingPage" />
        <activity android:name=".Login_Credential" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Login" />
        <activity
            android:name=".Registration"
            android:parentActivityName=".Login" />
    </application>

</manifest>